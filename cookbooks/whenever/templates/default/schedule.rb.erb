set :environment, :"<%= rails_env %>"

every 7.days, :at => '2:34 am' do
  runner "Activity.send_weekly_digests"
end

every 1.day, :at => '1:34 am' do
  runner "Activity.send_daily_digests"
end

every 1.day do
  rake "thinking_sphinx:index"
end

every :reboot do
  rake "thinking_sphinx:index"
end